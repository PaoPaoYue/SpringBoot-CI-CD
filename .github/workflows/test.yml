# This workflow will build a Java project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: test

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'environment profile name'
        required: true
        default: 'prod'
      version:
        description: 'docker version (default 0.0.1)'
        required: true
        default: '0.0.1'
      port:
        description: 'application port'
        required: true
        default: '5110'



jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - name: Set environment variables
      run: |
        echo "::set-env name=ENVIRONMENT::${{github.event.inputs.environment}}"
        echo "::set-env name=VERSION::${{github.event.inputs.version}}"
        echo "::set-env name=APP_PORT::${{github.event.inputs.port}}"

    - uses: actions/checkout@v2
    - name: deploy to remote server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.PRIVATE_SSH_HOST }}
        username: ${{ secrets.PRIVATE_SSH_USERNAME }}
        password: ${{ secrets.PRIVATE_SSH_PASSWORD }}
        script: | 
          echo "Hello world!"
          sudo docker docker stop ${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}}
          sudo docker docker rm ${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}}
          sudo docker docker rmi ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}}-${{env.VERSION}}
          sudo docker docker pull ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}}-${{env.VERSION}}
          sudo docker docker run -d -p ${{env.APP_PORT}}:${{env.APP_PORT}} -p 10200:10200 -e JAVA_OPTS=-Dspring.profiles.active=${{env.ENVIRONMENT}} --name=${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}} ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:${{env.ENVIRONMENT}}-${{env.VERSION}}

