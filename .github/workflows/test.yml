# This workflow will build a Java project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: test

on:
  workflow_dispatch



jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Set env to development
      if: endsWith(github.ref, '/dev')
      run: |
        echo "::set-env name=ENVIRONMENT::dev"
    - name: Set env to production
      if: endsWith(github.ref, '/master')
      run: |
        echo "::set-env name=ENVIRONMENT::prod"
    - name: deloy Docker image remotely
      uses: docker://evaneos/ssh-action:0.1.0
      with:
        hosts: ${{ secrets.PRIVATE_SSH_HOST }}
        user: ${{ secrets.PRIVATE_SSH_USER }}
        password: ${{ secrets.PRIVATE_SSH_PASSWORD }}
        commands: | 
          echo "Hello world!"
          echo $ENVIRONMENT
